# Урок 5. Иерархические матчеры и работа с параметрами

## Цель

Научиться использовать **иерархические матчеры** для точного поиска элементов внутри повторяющихся карточек. Освоить **параметризированные шаги** и **SharedSteps** для переиспользования общих сценариев.

---

## Часть 1. Проблема одинаковых тегов

### Повторяющиеся элементы в ленте

Откройте экран **Feed** (лента) в Layout Inspector. Вы увидите несколько карточек чая (`BrewingCard`), и **внутри каждой карточки одинаковые теги**:

| Элемент | testTag |
|:---|:---|
| Аватар автора | `feed_post_avatar` |
| Имя автора | `feed_post_author_name` |
| Дата | `feed_post_date` |
| Название чая | `feed_post_tea_name` |
| Тип чая | `feed_post_tea_type` |
| Вес | `feed_post_weight_grams` |
| Объём | `feed_post_volume_ml` |
| Посуда | `feed_post_vessel` |
| Проливы | `feed_post_infusions` |

Теги **одинаковы** для всех карточек — ведь они описывают **тип** элемента, а не конкретный экземпляр.

### Почему обычный поиск не работает

Если на экране 4 карточки, то есть 4 узла с тегом `feed_post_tea_name`. Простой поиск:

```kotlin
// ❌ Найдёт НЕСКОЛЬКО узлов → тест упадёт с ошибкой
composeTestRule
    .onNodeWithTag(Tags.FEED_POST_TEA_NAME)
    .assertIsDisplayed()
```

**Ошибка:**
```
Expected at most 1 node but found 4 nodes that satisfy (TestTag = 'feed_post_tea_name')
```

`onNodeWithTag` внутри вызывает `onNode(matcher)`, который **требует ровно один** совпавший узел. Если найдено 0 или 2+ — тест падает. Это явная ошибка, которую легко заметить.

### А если использовать Kakao Compose?

Казалось бы, в Kakao Compose проблема решена — тест не падает:

```kotlin
val postTeaName: KNode = child {
    hasTestTag(Tags.FEED_POST_TEA_NAME)
}

FeedScreen.postTeaName.assertIsDisplayed()  // ✅ Тест проходит! Но правильно ли?
```

`KNode` работает иначе: внутри он использует **`onAllNodes(matcher)[position]`** — находит **все** совпавшие узлы и берёт элемент по позиции (по умолчанию `position = 0`, т.е. **первый попавшийся**).

Это **опаснее**, чем падение теста! Тест проходит, но может проверять **не тот элемент**:


```kotlin
// ⚠️ Тест проверит ПЕРВУЮ карточку, а не карточку Елены!

    FeedScreen {
        postTeaName {
            assertIsDisplayed()
            assertTextEquals("Шен Пуэр")   // 💥 Упадёт — первым окажется другой чай
        }                                   // или хуже: пройдёт, если случайно совпадёт!
    }   
```

> [!CAUTION]
> **Тест, который проходит, но проверяет не тот элемент — хуже, чем тест, который падает.** Падающий тест сигнализирует о проблеме. «Зелёный» тест с неправильным элементом даёт **ложную уверенность**.

### Решение: иерархические матчеры

**Иерархические матчеры** позволяют уточнить поиск через **отношения** между узлами в семантическом дереве: «найди элемент, у которого **рядом** (сосед) есть другой элемент с определённым свойством».

---

## Часть 2. Семантическое дерево карточки

### Как устроено дерево

Каждая карточка `BrewingCard` в коде содержит вложенные `Column`, `Row`, `Box`. Но в **семантическом дереве**  промежуточные контейнеры без `semantics` прозрачны. Элементы с `testTag` становятся **соседями** — прямыми потомками одного общего предка (карточки):

> [!TIP]
> Помимо LayoutIncpector'a для получения иерархии можно использовать `printToLog()`.

```kotlin
// Вывести объединённое дерево (по умолчанию)
composeTestRule.onRoot().printToLog("TREE")

// Вывести необъединённое дерево
composeTestRule.onRoot(useUnmergedTree = true).printToLog("TREE_UNMERGED")
```

Пример вывода:

```
...
|     |-Node #95 at (...)px, Tag: 'feed_post_1'
|     | IsContainer = 'true'
|     | Shape = 'RoundedCornerShape(...)'
|     |  |-Node #99 at (...)px
|     |  | Shape = 'RoundedCornerShape(...)'
|     |  |  |-Node #100 at (...)px, Tag: 'feed_post_avatar'
|     |  |-Node #103 at (...)px, Tag: 'feed_post_author_name'
|     |  | Text = '[Мария]'
|     |  | Actions = [...]
|     |  |-Node #104 at (...)px, Tag: 'feed_post_date'
|     |  | Text = '[19 февр., 16:30]'
|     |  | Actions = [...]
|     |  |-Node #106 at (...)px, Tag: 'feed_post_tea_name'
|     |  | Text = '[Ассам]'
|     |  | Actions = [...]
|     |  |-Node #107 at (...)px, Tag: 'feed_post_tea_type'
|     |  | Text = '[Красный]'
|     |  | Actions = [...]
|     |  |-Node #110 at (...)px, Tag: 'feed_post_weight_grams'
|     |  | Text = '[7 г]'
|     |  | Actions = [...]
|     |  |-Node #111 at (...)px, Tag: 'feed_post_volume_ml'
|     |  | Text = '[200 мл]'
|     |  | Actions = [...]
|     |  |-Node #112 at (...)px, Tag: 'feed_post_vessel'
|     |    Text = '[Чайник]'
|     |    Actions = [...]

...
```

---

## Часть 3. Матчеры иерархии

### hasAnySibling — поиск через соседа

`hasAnySibling(matcher)` — проверяет, что у узла есть **сосед** (элемент с тем же родителем), удовлетворяющий условию.

```kotlin
import androidx.compose.ui.test.hasAnySibling
import androidx.compose.ui.test.hasTestTag
import androidx.compose.ui.test.hasText

// Найти "сорт чая" в карточке, где автор — "Мария"
        composeTestRule.onNode(
            hasTestTag(Tags.FEED_POST_TEA_NAME) and hasAnySibling(
                hasTestTag(Tags.FEED_POST_AUTHOR_NAME) and hasText("Мария")
            )
        ).assertTextEquals("Ассам")
```

Разберём по шагам:
1. `hasTestTag(Tags.FEED_POST_TEA_NAME)` — ищем узел с тегом названия чая
2. `hasAnySibling(...)` — у найденного узла должен быть сосед...
3. `hasTestTag(...) and hasText("Мария")` — ...с тегом автора и текстом «Мария»

Результат: из 7 узлов `feed_post_tea_name` выбирается **именно тот**, который находится рядом с «Мария».

### Комбинирование матчеров 

Матчеры могут комбинироваться инфиксными операторами `and`, `or`, `not`.

### Полный набор матчеров иерархии

| Матчер | Описание | Пример |
|:---|:---|:---|
| `hasAnySibling(m)` | Есть **сосед** (тот же родитель) | `hasAnySibling(hasText("Николай"))` |
| `hasParent(m)` | **Прямой родитель** соответствует | `hasParent(hasTestTag("card"))` |
| `hasAnyAncestor(m)` | **Любой предок** соответствует | `hasAnyAncestor(hasTestTag("card"))` |
| `hasAnyChild(m)` | Есть **прямой потомок** | `hasAnyChild(hasText("Сенча"))` |
| `hasAnyDescendant(m)` | Есть **любой потомок** | `hasAnyDescendant(hasText("5 г"))` |

> [!IMPORTANT]
> Внутри этих матчеров используются функции из `androidx.compose.ui.test` (`hasTestTag`, `hasText` и т.д.), которые возвращают `SemanticsMatcher`. Не путайте их с одноимёнными методами `ViewBuilder` в Kakao Compose — те добавляют матчер в список и возвращают `Unit`.

---

## Часть 4. ComposeScreen с иерархическими матчерами

### Page Object для ленты

В Kakao Compose `ViewBuilder` поддерживает все иерархические матчеры: `hasAnySibling`, `hasAnyAncestor`, `hasAnyChild`, `hasAnyDescendant`, `hasParent`. Мы можем использовать их прямо внутри `child { }`.

Дополним `FeedScreen` методами, где элементы ищутся через **имя автора** как якорь:

```kotlin
import io.github.kakaocup.compose.node.element.ComposeScreen
import io.github.kakaocup.compose.node.element.KNode
import com.example.kaspresso_learning.Tags
import androidx.compose.ui.test.hasTestTag as matchTag
import androidx.compose.ui.test.hasText as matchText

object FeedScreen : ComposeScreen<FeedScreen>() {

    // ...

    fun teaNameOnCard(authorName: String): KNode = child {
        hasTestTag(Tags.FEED_POST_TEA_NAME)
        hasAnySibling(matchTag(Tags.FEED_POST_AUTHOR_NAME) and matchText(authorName))
    }

    fun teaTypeOnCard(authorName: String): KNode = child {
        hasTestTag(Tags.FEED_POST_TEA_TYPE)
        hasAnySibling(matchTag(Tags.FEED_POST_AUTHOR_NAME) and matchText(authorName))
    }

    fun vesselOnCard(authorName: String): KNode = child {
        hasTestTag(Tags.FEED_POST_VESSEL)
        hasAnySibling(matchTag(Tags.FEED_POST_AUTHOR_NAME) and matchText(authorName))
    }

    fun weightOnCard(authorName: String): KNode = child {
        hasTestTag(Tags.FEED_POST_WEIGHT)
        hasAnySibling(matchTag(Tags.FEED_POST_AUTHOR_NAME) and matchText(authorName))
    }
}
```

### Зачем import alias

Обратите внимание на импорты:

```kotlin
import androidx.compose.ui.test.hasTestTag as matchTag
import androidx.compose.ui.test.hasText as matchText
```

Внутри `child { }` мы находимся в контексте `ViewBuilder`, у которого есть **свои** методы `hasTestTag()` и `hasText()`. Они добавляют матчер в список и возвращают `Unit`. Но `hasAnySibling(matcher)` ожидает аргумент типа `SemanticsMatcher`, а не `Unit`.

Чтобы внутри `hasAnySibling(...)` использовать функции из `androidx.compose.ui.test` (которые возвращают `SemanticsMatcher`), мы даём им **псевдонимы** через `import ... as`:

```kotlin
// ViewBuilder.hasTestTag → добавляет в список, возвращает Unit
hasTestTag(Tags.FEED_POST_TEA_NAME)
// androidx.compose.ui.test.hasTestTag → возвращает SemanticsMatcher
hasAnySibling(matchTag(Tags.FEED_POST_AUTHOR_NAME) and matchText(authorName))
//            ^^^^^^^^                                 ^^^^^^^^^
//            import alias                               import alias

Иначе это выглядело бы как:
hasAnySibling(androidx.compose.ui.test.hasTestTag(Tags.FEED_POST_AUTHOR_NAME) and androidx.compose.ui.test.hasText(authorName))
```

### Использование в тесте

```kotlin
step("Проверяем карточку Марии") {
    FeedScreen {
        teaNameOnCard("Мария") {
            assertIsDisplayed()
            assertTextEquals("Ассам")
        }
        vesselOnCard("Мария") {
            assertIsDisplayed()
            assertTextEquals("Чайник")
        }
    }
}
```

> Матчеры могут быть и более сложными, но это делает их более хрупкими и тяжелыми для понимания, поэтому все должно быть в меру. 

---

## Часть 5. Параметризированные шаги

### FeedSteps

Создадим Steps с параметрами — **имя автора** определяет карточку, остальные параметры — ожидаемые значения:

```kotlin
object FeedSteps {

    fun assertTeaDataOnCard(
        authorName: String,
        expectedTeaName: String,
        expectedTeaType: String,
        expectedVessel: String
    ) {
        FeedScreen {
            teaNameOnCard(authorName) {
                assertIsDisplayed()
                assertTextEquals(expectedTeaName)
            }
            teaTypeOnCard(authorName) {
                assertIsDisplayed()
                assertTextEquals(expectedTeaType)
            }
            vesselOnCard(authorName) {
                assertIsDisplayed()
                assertTextEquals(expectedVessel)
            }
        }
    }
}
```

Один вызов — проверка сразу трёх полей на конкретной карточке:

```kotlin
FeedSteps.assertTeaDataOnCard(
    authorName = "Мария",
    expectedTeaName = "Ассам",
    expectedTeaType = "Красный",
    expectedVessel = "Чайник"
)
```

---

## Часть 6. SharedSteps — переиспользуемые сценарии

### Проблема: повторяющееся предусловие

Почти каждый тест начинается с **логина**: выбрать аватар → нажать «Далее» → ввести имя → нажать «Войти». Мы уже писали эту логику один раз в LoginFlowTest, а в будущих тестах она будет повторяться снова и снова

Если флоу логина изменится — придётся править **каждый тест**.

### Решение: SharedSteps

**SharedSteps** — объекты, которые объединяют **шаги из разных экранов** в один переиспользуемый сценарий. В отличие от обычных Steps (привязанных к одному экрану), SharedSteps **оркестрируют** степы нескольких экранов.

```
┌──────────────────────────────────────────────┐
│  Уровни абстракции                           │
├──────────────────────────────────────────────┤
│  Screen       — ЧТО есть на экране           │
│  Steps        — КАК взаимодействовать        │
│  SharedSteps  — ЗАЧЕМ (бизнес-сценарий)      │
│  Test         — КОГДА (конкретный кейс)      │
└──────────────────────────────────────────────┘
```

### Структура директорий

```
androidTest/
├── screens/           ← Page Objects (элементы экранов)
│   ├── AvatarSelectScreen.kt
│   ├── NameInputScreen.kt
│   └── FeedScreen.kt
├── steps/             ← Steps (действия на одном экране)
│   ├── AvatarSelectSteps.kt
│   ├── NameInputSteps.kt
│   └── FeedSteps.kt
├── sharedSteps/      ← SharedSteps (кросс-экранные сценарии)
│   └── LoginSharedSteps.kt
├── tests/             ← Тесты
│   └── FeedCardsTest.kt
└── BaseTestCase.kt
```

### Реализация LoginSharedSteps

```kotlin
object LoginSharedSteps {

    /**
     * Полный флоу логина: выбор аватара → «Далее» → ввод имени → «Войти» → проверка открытия ленты.
     */
    fun login(avatarIndex: Int = 0, userName: String = "Тестер") {
        AvatarSelectSteps.selectAvatar(avatarIndex)
        AvatarSelectSteps.clickNextBtn()
        NameInputSteps.enterName(userName)
        NameInputSteps.clickLoginBtn()
        FeedScreen.assertTitleDisplayed()
    }
}
```

Обратите внимание:
- `LoginSharedSteps` **не знает** про теги, KNode и composeTestRule — он работает только через Steps
- Параметры по умолчанию (`avatarIndex = 0`, `userName = "Тестер"`) упрощают вызов
- Если флоу логина изменится — правка в **одном файле**

Вместо четырех шагов в каждом тесте мы пишем один вызов:

```kotlin
LoginSharedSteps.login()
```

> [!IMPORTANT]
> **Правило SharedSteps:** SharedStep объединяет Steps **разных** экранов. Если действие касается **одного** экрана — это обычный Step. Если пересекает **несколько** — SharedStep.

---

## Задание

### Шаг 1 — LoginSharedSteps

Создайте директорию `shared_steps/` рядом с `screens/` и `steps/`. В ней создайте `LoginSharedSteps.kt`:

- Метод `login(avatarIndex, userName)` с параметрами по умолчанию
- Внутри: вызовы `AvatarSelectSteps` и `NameInputSteps`

### Шаг 2 — FeedScreen

Дополните `screens/FeedScreen.kt` — Page Object для экрана ленты.

Элементы:
- `teaNameOnCard(authorName)` — находит чай через `hasAnySibling`
- `teaTypeOnCard(authorName)` — тип чая через `hasAnySibling`
- `weightOnCard(authorName)` — вес через `hasAnySibling`
- `volumeOnCard(authorName)` — объём через `hasAnySibling`
- `vesselOnCard(authorName)` — посуду через `hasAnySibling`

> [!IMPORTANT]
> Не забудьте про import alias! Внутри `hasAnySibling(...)` нужны функции из `androidx.compose.ui.test`, которые возвращают `SemanticsMatcher`. Используйте `import ... as matchTag` / `import ... as matchText`, чтобы избежать конфликта с одноимёнными методами `ViewBuilder`.

### Шаг 3 — FeedSteps

Создайте `steps/FeedSteps.kt` с методом:

```kotlin
fun assertDataOnCard(
    authorName: String,
    expectedTeaName: String,
    expectedTeaType: String,
    expectedWeight: String,
    expectedVolume: String,
    expectedVessel: String
)
```

### Шаг 4 — Тест

Напишите тест `FeedCardsTest.kt`, который:

1. Логинится через `LoginSharedSteps.login()`
2. Проверяет данные на **трёх карточках**, например:
   - Карточка Алексея: Сенча, Зелёный, 5г, 150мл, Гайвань
   - Карточка Марии: Ассам, Красный, 7г, 200мл, Чайник
   - Карточка Дмитрия: Да Хун Пао, Улун, 8г, 250мл, Гайвань

---

## 📚 Полезные ссылки

- [Шпаргалка по тестированию Compose](https://developer.android.com/develop/ui/compose/testing/testing-cheatsheet?hl=ru)