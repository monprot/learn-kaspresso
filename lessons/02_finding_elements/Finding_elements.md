# Урок 2. Поиск элементов и первый тест

## Цель

Научиться находить UI-элементы с помощью Layout Inspector и по `testTag`, понять роль семантического дерева в Compose-тестировании, а затем написать свой первый Compose-тест.

---

## Часть 1. Как Compose «видит» UI — семантическое дерево

Прежде чем искать элементы для тестов, важно понять **как** тест-фреймворк находит элементы на экране.

### Что такое семантическое дерево

В Compose рядом с деревом компонентов (то, что рисуется на экране) существует **параллельное дерево — дерево семантики** (Semantics Tree). Оно описывает UI не визуально, а **по смыслу**:

- Какой это элемент? (кнопка, поле ввода, переключатель)
- Какой у него текст?
- Какое у него состояние? (включён/выключен, выбран/не выбран)
- Можно ли с ним взаимодействовать? (клик, ввод текста)

> [!IMPORTANT]
> **Именно семантическое дерево использует тест-фреймворк** для поиска элементов, проверки их свойств и выполнения действий. Не визуальное дерево компонентов, а семантическое дерево семантики.

### Зачем это нужно тестировщику

Когда вы пишете:

```kotlin
composeTestRule.onNodeWithTag("my_button").performClick()
```

тест-фреймворк ищет **узел в семантическом дереве** с атрибутом `TestTag = "my_button"`. Если такого узла нет — тест упадёт.

### Семантические свойства

Каждый узел семантического дерева может содержать набор **семантических свойств**:

| Свойство | Что означает | Пример |
|---|---|---|
| `TestTag` | Тег для тестов | `"avatar_icon_0"` |
| `Text` | Текстовое содержимое | `"Далее"` |
| `ContentDescription` | Описание для accessibility | `"Кнопка назад"` |
| `Role` | Роль элемента | `Button`, `Image`, `Checkbox` |
| `StateDescription` | Описание состояния | `"Включено"`, `"Выбрано"` |
| `ToggleableState` | Состояние переключателя | `On`, `Off` |
| `Focused` | В фокусе или нет | `true`, `false` |

---

## Часть 2. Layout Inspector — ваш главный инструмент

### Что такое Layout Inspector

**Layout Inspector** — встроенный инструмент Android Studio, который позволяет в **реальном времени** изучать дерево UI-элементов работающего приложения, включая **семантические свойства** каждого элемента. Для Compose-тестирования это основной инструмент разведки: вы видите структуру экрана, теги, тексты и роли элементов.

### Как запустить Layout Inspector

#### Способ 1 — Встроенный режим (рекомендуемый)

1. **Запустите приложение** на эмуляторе или устройстве
2. Откройте окно **Running Devices** (View → Tool Windows → Running Devices)
3. Нажмите кнопку **Toggle Layout Inspector**  на панели инструментов окна Running Devices
4. Layout Inspector активируется прямо поверх превью приложения

#### Способ 2 — Через меню

1. **Запустите приложение** на эмуляторе или устройстве
2. Откройте **Tools → Layout Inspector** в Android Studio
3. Выберите процесс вашего приложения в выпадающем списке

> [!TIP]
> Встроенный режим (Embedded Layout Inspector) — режим по умолчанию, где инспектор работает прямо в окне Running Devices. Его можно отключить через **File → Settings → Tools → Layout Inspector** → снять флажок «Enable embedded Layout Inspector».

### Основные панели и элементы интерфейса

Layout Inspector состоит из трёх ключевых панелей:

| Панель | Что показывает |
|---|---|
| **Component Tree** (дерево компонентов) | Иерархия всех UI-элементов. Для Compose-приложений показывает composable-функции |
| **Layout Display** (превью макета) | Визуальное отображение экрана приложения. Можно кликать на элементы прямо здесь |
| **Attributes** (атрибуты) | Все свойства выбранного элемента, включая семантику, размеры, модификаторы |

### Пошаговая инструкция: как найти `testTag` элемента

1. **Запустите приложение и Layout Inspector** (одним из способов выше)
2. Включите режим **Deep Inspect** (кнопка на панели инструментов) — это позволяет кликать на элементы прямо в превью
3. **Кликните на элемент** в превью или в Component Tree
4. В панели **Attributes** найдите раздел **Semantics** (или **Declared** / **Merged**)
5. Ищите свойство **`TestTag`** — это и есть строка, по которой тест находит элемент

> [!IMPORTANT]
> Если режим Deep Inspect включён, вы не сможете взаимодействовать с приложением (нажимать кнопки, скроллить). Чтобы вернуться к нормальному использованию — отключите Deep Inspect.

### Продвинутые возможности Layout Inspector

#### Сохранение и экспорт снимков

Snapshot-ы Layout Inspector можно сохранять и делиться ими:
- **Export Snapshot** — сохраняет иерархию, 3D-рендер и атрибуты
- **Import Snapshot** — загружает ранее сохранённый снимок для изучения

Это полезно, когда нужно обсудить структуру UI с коллегами или сохранить «до/после» при рефакторинге.

#### Фильтрация семантических слоёв

В Component Tree можно фильтровать узлы через **View Options** → **Highlight Semantics Layers**. Это выделит только те узлы, которые содержат семантику, что упрощает навигацию.

#### 3D-режим

Для сложных вложенных макетов Layout Inspector поддерживает 3D-визуализацию:
1. Сделайте снимок (кнопка  **Snapshot**) или отключите встроенный режим (Embedded Layout Inspector)
2. Нажмите кнопку **3D Mode**
3. Вращайте макет перетаскиванием мыши

Это помогает понять, какие слои перекрывают друг друга.

> К сожалению, в будующих версиях Android Studio эту возможность уберут.

---

## Часть 3. testTag — основная «ручка» для автотестов

### Что такое testTag

`testTag` — это **семантическое свойство**, которое вручную присваивается UI-элементу, чтобы тесты могли его однозначно найти. Его можно считать аналогом `id` в XML-вёрстке.

Думайте о тегах как о «ручках», за которые тест «хватает» элементы. Если у элемента есть тег — тест найдёт его надёжно. Если тега нет — придётся искать по тексту или другим свойствам, что менее надёжно.

В нашем проекте теги собраны в объекте `Tags` (файл `Tags.kt`). При работе с Layout Inspector вы уже видели эти теги в атрибутах элементов.

### Способы поиска элементов в тестах

Тест-фреймворк предоставляет несколько способов найти узел в семантическом дереве:

| Метод | Что ищет | Пример |
|---|---|---|
| `onNodeWithTag(tag)` | По `testTag` | `onNodeWithTag("my_button")` |
| `onNodeWithText(text)` | По точному тексту | `onNodeWithText("Далее")` |
| `onNodeWithText(text, substring = true)` | По подстроке | `onNodeWithText("Дал", substring = true)` |
| `onNodeWithContentDescription(desc)` | По `contentDescription` | `onNodeWithContentDescription("Назад")` |
| `onAllNodesWithTag(tag)` | Все узлы с тегом (возвращает список) | `onAllNodesWithTag("avatar_icon")` |

> [!IMPORTANT]
> Не все элементы уже имеют `testTag`. Если тега нет — тест не сможет найти элемент по `onNodeWithTag`. В следующем уроке мы столкнёмся с этой проблемой и научимся добавлять теги самостоятельно.

---

## Задание A (Закрепляем Layout Inspector)

1. Запустите приложение на эмуляторе
2. Откройте Layout Inspector (любым из двух способов)
3. Включите **Deep Inspect** и покликайте по элементам первого экрана (выбор аватара)
4. Для каждого из этих элементов найдите в панели **Attributes** свойство **`TestTag`**:
   - Контейнер экрана → `avatar_screen_container`
   - Любой аватар → `avatar_icon_0`, `avatar_icon_1` и т.д.
   - Кнопка «Далее» → `avatar_screen_next_button`
5. Выключите Deep Inspect, выберите аватар и нажмите «Далее» — перейдите на экран ввода имени
6. Снова включите Deep Inspect и найдите атрибуты на новом экране
7. **Найдите кнопку «Войти»** на `NameInputScreen` и убедитесь, что у неё **нет** `TestTag`
8. Откройте `Tags.kt` и `AvatarSelectScreen.kt` в коде — найдите, где задаётся `testTag`

---

## Часть 4. Первые тесты

### Что потребуется

- `onNodeWithTag(...)` — для поиска элемента по testTag
- `onNodeWithText(...)` — для поиска элемента по тексту
- `.assertIsDisplayed()` — для проверки, что элемент виден на экране
- `.assertTextEquals(...)` — для проверки, что текст соответствует ОР
- `.performClick()` — для нажатия на элемент

> [!TIP]
> Перед выполнением Action и большинства Assertion, рекомендуется всегда выполнять `.assertIsDisplayed()`, чтобы убедиться, что элемент виден. Это позволит избежать ошибок и сделать тесты более надежными.
---

##  Тест 1:

В файле `AvatarSelectScreenTest.kt` напишите новый тест, который покрывает следующий кейс:

**Шаги:**
1. На стартовом экране нажать кнопку «Далее» **без выбора** аватара

**Ожидаемый результат:**
- Появляется сообщение об ошибке `Сначала выберите аватар!`

---

Разберём кейс по шагам:

1. Нажать кнопку «Далее» (`avatar_screen_next_button`)
2. Проверить текст ошибки «Сначала выберите аватар!» через тег `name_input_screen_title` и Assertion `assertTextEquals`

## ⭐ Тест 2:

Создайте файл `NameEditScreenTest.kt` и напишите тест, который покрывает следующий кейс:

**Шаги:**
1. Выбрать любой из аватаров
2. Нажать кнопку «Далее»

**Ожидаемый результат:** 

Открылся экран ввода имени со всеми элементами:
- В заголовке текст «Как вас зовут?»
- Поле ввода имени отображается
- Кнопка «Войти» отображается

---

Разберём кейс по шагам:

1. Нажать на аватар `avatar_icon_2`
2. Нажать кнопку «Далее» (`avatar_screen_next_button`)
3. Проверить текст заголовка «Как вас зовут?» через тег `name_input_screen_title` и `assertTextEquals`
4. Проверить наличие поля ввода `name_input_screen_edit_text`
5. Проверить наличие кнопки с текстом «Войти» через `onNodeWithText`

---

## Подсказки

<details>
<summary>Подсказка 1: Проверка текста через assertTextEquals</summary>

Если у элемента есть `testTag`, можно найти его по тегу и проверить текст:

```kotlin
composeTestRule
    .onNodeWithTag(Tags.AVATAR_ERROR)
    .assertIsDisplayed()
    .assertTextEquals("Сначала выберите аватар!")
```

`assertTextEquals` проверяет, что текст узла **точно совпадает** с переданной строкой. Не забудьте добавить импорт `assertTextEquals`.

> [!TIP]
> `assertTextEquals` строже, чем `onNodeWithText` — она не просто ищет узел, а проверяет конкретное значение у уже найденного узла.

</details>

<details>
<summary>Подсказка 2: Переход между экранами</summary>

Для перехода нужно выполнить действие на текущем экране:

```kotlin
step("Нажать на второй аватар") {
    composeTestRule
        .onNodeWithTag("${Tags.AVATAR_ICON}_2")
        .assertIsDisplayed()
        .performClick()
}

step("Нажать 'Далее'") {
    composeTestRule
        .onNodeWithTag(Tags.AVATAR_NEXT_BUTTON)
        .assertIsDisplayed()
        .performClick()
}
```

После этого Compose автоматически отрисует новый экран.

</details>



<details>
<summary>Подсказка 3: Поиск элемента по тексту</summary>

Если у элемента **нет** `testTag` (как у кнопки «Войти»), его можно найти по тексту:

```kotlin
composeTestRule
    .onNodeWithText("Войти")
    .assertIsDisplayed()
```

`onNodeWithText` ищет узел с точным текстом в семантическом дереве.

</details>


---

## 📚 Полезные ссылки

- [Layout Inspector — документация Android](https://developer.android.com/studio/debug/layout-inspector?hl=ru)
- [Отладка Compose UI — документация Android](https://developer.android.com/develop/ui/compose/tooling/debug?hl=ru)
- [Шпаргалка по тестированию — документация Android](https://developer.android.com/develop/ui/compose/testing/testing-cheatsheet?hl=ru)

---

## Решение

<details>
<summary>AvatarSelectScreenTest.kt</summary>

```kotlin
package com.example.kaspresso_learning

import androidx.compose.ui.test.assertIsDisplayed
import androidx.compose.ui.test.assertTextEquals
import androidx.compose.ui.test.hasTestTag
import androidx.compose.ui.test.junit4.createAndroidComposeRule
import androidx.compose.ui.test.onNodeWithTag
import androidx.compose.ui.test.performClick
import com.kaspersky.kaspresso.testcases.api.testcase.TestCase
import org.junit.Rule
import org.junit.Test

class AvatarSelectScreenTest : TestCase() {

    @get:Rule
    val composeTestRule = createAndroidComposeRule<MainActivity>()

    @Test
    fun checkAvatarScreen() = run {
        step("Проверить, что экран выбора аватара отображается") {
            composeTestRule
                .onNode(hasTestTag(Tags.AVATAR_CONTAINER))
                .assertIsDisplayed()
        }

        step("Проверить, что отображаются 4 иконки аватаров") {
            for (i in 0..3) {
                composeTestRule
                    .onNodeWithTag("${Tags.AVATAR_ICON}_$i")
                    .assertIsDisplayed()
            }
        }

        step("Проверить, что кнопка «Далее» отображается") {
            composeTestRule
                .onNodeWithTag(Tags.AVATAR_NEXT_BUTTON)
                .assertIsDisplayed()
        }
    }

    @Test
    fun checkAvatarError() = run {
        step("Нажать на кнопку 'Далее'") {
            composeTestRule
                .onNodeWithTag(Tags.AVATAR_NEXT_BUTTON)
                .assertIsDisplayed()
                .performClick()
        }

        step("Проверить отображение ошибки") {
            composeTestRule
                .onNodeWithTag(Tags.AVATAR_ERROR)
                .assertIsDisplayed()
                .assertTextEquals("Сначала выберите аватар!")
        }
    }
}

```

</details>

<details>
<summary>NameEditScreenTest.kt</summary>

```kotlin
package com.example.kaspresso_learning

import androidx.compose.ui.test.assertIsDisplayed
import androidx.compose.ui.test.assertTextEquals
import androidx.compose.ui.test.junit4.createAndroidComposeRule
import androidx.compose.ui.test.onNodeWithTag
import androidx.compose.ui.test.onNodeWithText
import androidx.compose.ui.test.performClick
import com.kaspersky.kaspresso.testcases.api.testcase.TestCase
import org.junit.Rule
import org.junit.Test

class NameEditScreenTest : TestCase() {

    @get:Rule
    val composeTestRule = createAndroidComposeRule<MainActivity>()

    @Test
    fun checkNameEditScreen() = run {
        step("Нажать на второй аватар") {
            composeTestRule
                .onNodeWithTag("${Tags.AVATAR_ICON}_2")
                .assertIsDisplayed()
                .performClick()
        }

        step("Нажать 'Далее'") {
            composeTestRule
                .onNodeWithTag(Tags.AVATAR_NEXT_BUTTON)
                .assertIsDisplayed()
                .performClick()
        }

        step("Проверить, что открылся экран ввода имени") {

            step("Проверить заголовок «Как вас зовут?»") {
                composeTestRule
                    .onNodeWithTag(Tags.NAME_EDIT_TITLE)
                    .assertIsDisplayed()
                    .assertTextEquals("Как вас зовут?")
            }

            step("Проверить отображение поля ввода имени") {
                composeTestRule
                    .onNodeWithTag(Tags.NAME_EDIT_TEXT)
                    .assertIsDisplayed()
            }

            step("Проверить отображение кнопки 'Войти'") {
                composeTestRule
                    .onNodeWithText("Войти")
                    .assertIsDisplayed()
            }
        }
    }
}
```

</details>

---


